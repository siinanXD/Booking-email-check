[build]
builder = "nixpacks"
buildCommand = "cd frontend && npm install && npm run build && cd .."

[deploy]
startCommand = "gunicorn 'backend.api.app:create_app()' --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 120"
